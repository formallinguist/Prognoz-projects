ssh keys provide a secure communication with github. Whereas hhtps uses password inputs, ssh keys consist of a public/private key pair, which for example makes them less  assailable regarding brute-force attacks. The cryptographic handshake -generating a key pair locally and storing the public key on github -ensures that only authorized users with the private key can access repositories
